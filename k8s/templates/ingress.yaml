{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "better-tts-studio.fullname" . }}-ingress
  namespace: {{ .Values.namespace | default "vieneu-studio" }}
  labels:
    app.kubernetes.io/name: {{ include "better-tts-studio.name" . }}
    helm.sh/chart: {{ include "better-tts-studio.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts:
        - {{ .Values.ingress.host | quote }}
      secretName: {{ include "better-tts-studio.fullname" . }}-tls
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "better-tts-studio.fullname" . }}-backend
                port:
                  number: {{ .Values.backend.service.port }}
{{- end }}
