# name: VieNeu TTS API CI
# 
# on:
#   push:
#     branches: [ main ]
# 
# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
# 
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
# 
#       - name: Log in to Harbor
#         uses: docker/login-action@v3
#         with:
#           registry: harbor.amoratran.id.vn
#           username: ${{ secrets.HARBOR_USERNAME }}
#           password: ${{ secrets.HARBOR_PASSWORD }}
# 
#       - name: Build and Push Docker Image
#         run: |
#           IMAGE_NAME="harbor.amoratran.id.vn/tiny-src/vieneu-tts-api"
#           TAG="${{ github.sha }}"
#           
#           # Build image
#           docker build -t $IMAGE_NAME:$TAG -t $IMAGE_NAME:latest .
#           
#           # Push image tags to Harbor
#           docker push $IMAGE_NAME:$TAG
#           docker push $IMAGE_NAME:latest
# 
#       - name: Update Image Tag in Helm values and Push to Git
#         run: |
#           TAG="${{ github.sha }}"
#           
#           # Cập nhật tag mới
#           sed -i -E "s/tag: .*/tag: \"$TAG\"/g" k8s/values.yaml
#           
#           # Cấu hình Git
#           git config --global user.name "Gitea Actions"
#           git config --global user.email "actions@gitea.org"
#           
#           # Commit thay đổi
#           git add k8s/values.yaml
#           git commit -m "chore: update image tag to $TAG [skip ci]" || exit 0
#           
#           # Tránh lỗi push bị từ chối bằng cách pull --rebase trước khi push
#           git pull --rebase origin main
#           git push origin HEAD:main
